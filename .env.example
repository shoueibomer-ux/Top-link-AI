DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2
DB_NAME=toplinkai
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
ANTHROPIC_API_KEY=
GOOGLE_PLACES_API_KEY=
REDIS_URL=redis://127.0.0.1:6379/1
API_KEY=dev-local-shared-key
CORS_ALLOWED_ORIGINS=
# Set to True only if this really is deployed behind a reverse proxy (nginx,
# a load balancer) that terminates TLS and sets X-Forwarded-Proto itself —
# see config/settings.py's SECURE_PROXY_SSL_HEADER comment for why this is
# unsafe to enable otherwise.
DJANGO_BEHIND_PROXY=False
